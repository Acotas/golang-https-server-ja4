# golang-https-server-ja4

golang https server obtains the browser's JA4 fingerprint, without third-party libraries or underlying packaging, saving it directly in the Context, and not saving it globally.

Go语言编写的HTTPS服务器获取浏览器的JA4指纹，不需要第三方库，不需要包装底层，直接保存在Context中，不需要全局保存